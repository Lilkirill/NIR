# Optional dependencies for local Qwen2.5-3B-Instruct inference and LoRA/QLoRA fine-tuning.
# Keep requirements.txt unchanged for lightweight API tests and Docker startup.
torch>=2.3.0
transformers>=4.46.0
accelerate>=0.33.0
peft>=0.12.0
datasets>=2.20.0
trl>=0.9.6
bitsandbytes>=0.43.0; platform_system != "Windows"
safetensors>=0.4.3
